{
  "name": "cryptocroc-runner",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone runner system for Discord trade signals and telemetry-driven strategy optimization.",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "check": "node --check api/trade-funnel.js && node --check api/public-latest.js && node --check api/trade-history.js && node --check api/trade-stats.js",
    "check:runner": "node --check lib/tradeSystem.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/kv": "^1.0.1",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "vercel": "^39.0.0"
  }
}